import React from "react";
import ReactLoading from "react-loading";

function Load() {
    return (
        <div>
              <ReactLoading type="bars" color="#0000FF"
                height={100} width={50} />
        </div>
    );
}
export default Load()
